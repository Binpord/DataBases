SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;

BEGIN TRAN;

SELECT * FROM JOB;

UPDATE JOB SET
	job_id = job_id + 4
	WHERE [function] = N'менеджер';

SELECT * FROM JOB;
	
WAITFOR DELAY '00:00:10';

COMMIT;